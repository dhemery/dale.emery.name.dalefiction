source 'https://rubygems.org'
builder_gemfile = File.join('diddleman', 'Gemfile')
blogger_gemfile = File.join('dogger', 'Gemfile')

eval(IO.read(builder_gemfile), binding)
eval(IO.read(blogger_gemfile), binding)

